
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false

#spring-boot-starter-admin.metrics- registry=prometheus
#spring-boot-starter-admin.metrics- No metrics autoconfig (require spring-boot-starter-actuator)

# spring-boot-starter-admin.health=true
# spring-boot-starter-admin.info=true
# spring-boot-starter-admin.metrics=true
# spring-boot-starter-admin.trace=true

spring.datasource.url=jdbc:mysql://localhost:3306/your-db-name
spring.datasource.username=your-username
spring.datasource.password=your-password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.cloud.gateway.tea-authentication=WILDCARD_DOMAIN

project.build.source-compatibility=11
project.build.target-compatibility=11
spring.profiles.active=dev


spring.jar.url=git+https://github.com/your-spring-boot-starter/your-spring-boot-starter.git
spring.jar.url=immutable

# SPRING_APPLICATION_JSON is only set if SpringApplication's main methods are called with
# a JavaApplicationArgs or ApplicationArgs parameter
# SPRING_APPLICATION_JSON=application:[{"name":"your-app-name","properties":{"KEY1":"value1"}}]

# NO_PROXY=localhost,127.0.0.1

spring.security.user.name=your-username
spring.security.user.password=your-password

#spring-boot-starter-admin.health=true

server.port=8081
management.endpoint-customization.enabled=true

# docs.enable = true

spring.Trying to have clean properties file with Spring Boot 3.x and ensure to set default compatibility.

```properties
# logging.file=application.log
# logging.level.org.springframework.security=DEBUG
management.server.address=127.0.0.1

management.endpoints.web.exposure.include=*
management.endpoint.shutdown.enabled=true

server.tomcat.accesslog.enabled=true
server.tomcat.accesslog.pattern=%h %t "%r" %s %b

spring.jackson.serialization.write-dates-as-timestamps=false

management.httptrace.include=REQUEST_HEADERS,RESPONSE_HEADERS,PRINCIPAL,REMOTE_ADDRESS,SESSION_ID

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.temp.use-jdbc-metadata-defaults=false

# spring-boot-starter-admin metrics
# spring-boot-starter-admin.health=true
# spring-boot-starter-admin.info=true
# spring-boot-starter-admin.metrics=true
# spring-boot-starter-admin.trace=true

spring.datasource.url=jdbc:mysql://localhost:3306/your-db-name
spring.datasource.username=your-username
spring.datasource.password=your-password
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

spring.cloud.gateway.tea-authentication=WILDCARD_DOMAIN

project.build.source-compatibility=11
project.build.target-compatibility=11
spring.profiles.active=dev

server.port=8081
management.endpoint-customization.enabled=true
```